%p#notice= notice
=link_to "Initiate Server Check", init_roll_roll_path(1)
%table
  %thead
    %tr
      %th Name
      %th{:colspan => "3"}
  %tbody
    - @rolls.each do |r|
      %tr
        %td
        %td
          = render 'form',  r: r
        /%td= roll.name
        /%td= link_to 'Show', roll
        /%td= link_to 'Edit', edit_roll_path(roll)
        /%td= link_to 'Destroy', roll, method: :delete, data: { confirm: 'Are you sure?' }
/  = link_to 'New Roll', new_roll_path

